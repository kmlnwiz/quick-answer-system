# Database
# PostgreSQL接続URL
# ローカル開発環境の例: postgresql://postgres:postgres@localhost:5432/quiz_dev
# 本番環境: クラウドデータベースのURLを使用
DATABASE_URL="postgresql://user:password@localhost:5432/quiz_system"

# Pusher（リアルタイム通信）
# https://dashboard.pusher.com/ でアカウント作成後に取得
PUSHER_APP_ID="your_pusher_app_id"
PUSHER_SECRET="your_pusher_secret"
NEXT_PUBLIC_PUSHER_KEY="your_pusher_key"
NEXT_PUBLIC_PUSHER_CLUSTER="ap3"

# 管理者認証
# ADMIN_PASSWORD: 管理画面 (/admin) へのログインパスワード
#   - 開発環境: 任意のパスワード（例: admin123）
#   - 本番環境: 強固なパスワードを設定すること
ADMIN_PASSWORD="your_secure_password"

# SESSION_SECRET: セッション暗号化用のシークレットキー
#   - 32文字以上のランダムな文字列を使用
#   - 生成例: openssl rand -base64 32
SESSION_SECRET="your_random_32_characters_or_more_secret_key"
